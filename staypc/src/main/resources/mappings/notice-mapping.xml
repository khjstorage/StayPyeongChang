<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="notice">
	<select id="notice_list" resultType="noticeDTO">
		select * from STAYPC_NOTICE
	</select>
	
	<select id="notice_view">
		select * from STAYPC_NOTICE where list_no=#{list_no}
	</select>
	
	<insert id="notice_insert">
		insert into board values((select nvl(max(list_no),1)+1 from board),#{subject},#{content},#{id},sysdate)
		insert into STAYPC_NOTICE values((select nvl(max(list_no),1)+1 from STAYPC_NOTICE),#{subject},#{content},#{id},sysdate)
	</insert>
	
	<update id="notice_update">
		update STAYPC_NOTICE set CONTENT=#{content}, subject=#{subject} where list_no=#{list_no}
	</update>
	
	<delete id="notice_delete" parameterType="noticeDTO">
		delete STAYPC_NOTICE where list_no=#{list_no}
	</delete>

</mapper>
